# GuardTalkOS WebInstaller — DECISION_LOG

Format: one entry per decision. `ID | date | owner | status | decision | source`.
Statuses: `TAKEN` (decided, binding), `OPEN` (awaiting human), `SUPERSEDED`.

---

| ID | Date | Owner | Status | Decision | Source |
|----|------|-------|--------|----------|--------|
| D-001 | 2026-08-22 | Human (build prompt) | TAKEN | `/install/recover` is the honest lost-key path: unlock (wipes all data) → re-enrol a new key → reinstall. Data loss stated plainly on the page. Recovery touches nothing else. | Build prompt §DECISIONS |
| D-002 | 2026-08-22 | Human (build prompt) | TAKEN | `/install/verify-device` exposes steps 5 and 8 only: read enrolled key hash from device where bootloader exposes it, or guide boot-screen fingerprint comparison; report match/mismatch. | Build prompt §DECISIONS |
| D-003 | 2026-08-22 | Human (build prompt) | TAKEN | `/install/update` runs steps 1, 2, 3 (flow 2 or 3 ONLY — key already exists; "generate" hidden with reason given), 4, 5 (no unlock; sideload path `// confirm`), 6, 8, 9. Any `generate` reachability on `/install/update` is a build-breaking lint failure. | Build prompt §DECISIONS |
| D-004 | 2026-08-22 | Human (build prompt) | TAKEN | GuardTalkOS ships NO automatic OTA path while `RELEASE_STATE=alpha`. Updates arrive as full release packages the user downloads over Tor, verifies, re-signs with their own key, and installs via `/install/update`. A future in-OS updater (must verify against user key + on-device confirmation) is recorded as a design constraint in ARCHITECTURE.md, not a shipped feature. | Build prompt §UPDATE POLICY AS CODE |
| D-005 | 2026-08-22 | Human (build prompt) | TAKEN | Three key flows offered as equals — generate here / bring your own / sign elsewhere. The page never defaults silently; no pre-selected flow. | Build prompt §KEY MODEL |
| D-006 | 2026-08-22 | Human (build prompt) | TAKEN | Installer is a static page with ZERO network access at runtime. CSP: `default-src 'none'; script-src 'self'; style-src 'self'; img-src 'self' data:; connect-src 'none'`. No-connect CI test gates the build. | Build prompt §RUNTIME |
| D-007 | 2026-08-22 | Human (build prompt) | TAKEN | No key material ever leaves the browser tab: no URL, cookie, localStorage (except opt-in encrypted "remember", off by default), log line, error message, or crash report. Private keys live in non-extractable WebCrypto handles where possible, memory otherwise, zeroed on unload where platform permits. | Build prompt §KEY CUSTODY |
| D-008 | 2026-08-22 | Human (session directive) | TAKEN | `ultimate_critique` is skipped for this project (MCP outage, human-approved skip). `self_critique` (strict) is the Gate-5 substitute; Law-5 scanner flag treated as known noise. Never fabricate scores. | Session governance record 2026-08-22 |
| D-009 | 2026-08-22 | Agent (conflict log) | OPEN→ESCALATED | Sources #1 (GuardTalkOS_Web_Brief_v1_0.md), #2 (GuardTalkOS_Claude_Code_Build_Prompt_v1_0.md), #4 (guardtalk_brand_book.html) are NOT present in this repo. Per prompt rule ("never resolve an open question by inventing"), identity tokens, register rules, claim-discipline layer, and status.json contract cannot be inherited from absent files. Foundations (AVB/fastboot/keys/verify/state machine/tests) proceed — they depend only on sources #3 (Executive Summary, present) and #5 (this prompt). Screen copy/styling phases blocked pending human answer in QUESTIONS_FOR_HUMAN.md Q-01..Q-03. | Repo survey 2026-08-22 |
| D-010 | 2026-08-22 | Agent | TAKEN | Reference `avbtool` for parity fixtures is `/mnt/Big-Storage/GuardTalk/GrapheneOS-worktree/external/avb/avbtool.py` (present, verified). Parity tests generate deterministic sample vbmeta via avbtool invocations and compare our TypeScript signer byte-for-byte. | Repo survey 2026-08-22 |
| D-011 | 2026-08-22 | Agent (synthesis of prompt §DEFINITION OF DONE + standing DEC-009) | TAKEN | **DEC-009 reconciliation:** the installer implements the COMPLETE fastboot command set and full flash orchestration so the product is capable of real flashes when operated by a human; however, every automated test, CI gate and this session's end-to-end runs execute exclusively against simulated/dry-run transports (no physical device exists in-session — see Q-11). The prior dry-run-only HOLD is preserved for all machine-driven execution; lifting it for real-hardware operation is an explicit human launch-time action, listed among the flags to flip in PROGRESS.md. No code path may claim a live flash that did not occur (`LIVE_FLASH_CLAIMED` stays false until a human flips launch flags with a real device). | Reader C survey + build prompt DoD |
| D-012 | 2026-08-22 | Agent | OPEN→ESCALATED | Device allowlist discrepancy: existing `wizard/devices.ts` allows `tokay` + `akita` (rango marked hidden/experimental per WEB_INSTALLER.md ADR); Executive Summary §3 grounds `tokay` + `rango` as the shipping pair. New installer targets {`tokay`, `rango`} first-class per Exec Summary; `akita` stays behind an explicit experimental flag pending confirmation (Q-14). No codename invented beyond these three. | Reader C digest §5 vs Executive Summary §3 |
| D-013 | 2026-08-22 | Agent | OPEN→ESCALATED | Physical home of `/install`: no os.guardtalk.io site tree exists anywhere in the repo (Reader C verdict). Adopted provisionally: **extend `vendor/guardtalk/web-installer/` into a statically-exported multi-route site** (Option A) — reuses the tested flashcore module graph and token inventory, zero new framework dependencies (Law 22), matches existing tsc/tsx toolchain. Route group emits `/install`, `/install/update`, `/install/verify-device`, `/install/recover`. Revisit if the human supplies the absent site build prompt with a mandated stack (Q-02). | Reader C digest §2 vs Executive Summary §3 |
| D-014 | 2026-08-23 | Architect | TAKEN | Direct fix to `lib/avb/signer.ts` rawRsaSign path (outside all builder scopes at time of fix): the KeyMaterial signing branch fed the bare 32-byte SHA-256 digest into the textbook-RSA primitive, throwing `EM length 32 != modulus length 512`. Now builds the full EMSA-PKCS1-v1_5 EM (`00 01 ‖ FF×458 ‖ 00 ‖ DER DigestInfo ‖ digest`) exactly per avbtool.py:474 before the RSA op; WebCrypto CryptoKey path unchanged (already parity-correct). Root cause of Route Builder A's 5 crash-class test failures. AVB parity suite re-verified 17/17 after fix. Phase-2 gap explanation: parity suite exercised only the CryptoKey branch. Law 6 minimal change: one call-site + one pure helper. | Phase-3 resume triage 2026-08-23 |

## Conflict-resolution order applied (per working agreements)

Brand Book → identity · OS brief → routes/copy/rules · Executive Summary → product facts · build prompt → installer behaviour/orchestration.
Every actual conflict encountered is logged above with an ID; none may be silently resolved.
