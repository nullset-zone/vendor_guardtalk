# Komodo (Pixel 9 Pro XL) Port Preflight

> **Task:** `T-PORT-KOMODO-PREFLIGHT`  
> **Date:** 2026-09-14  
> **Decision:** DEC-PORT-KOMODO-001 / DEC-PORT-KOMODO-003 — komodo after tokay/akita; this card = prerequisites only  
> **Scope:** Prerequisites only. GuardTalk device layer = `T-PORT-KOMODO-LAYER`. Flash bundle = `T-PORT-KOMODO-FLASH`.  
> **Verdict:** **GENERATE SUCCESS.** Live `vendor/google_devices/komodo/` exists. Factory staged. Lunch **FAIL** on missing `trunk-14096387` (expected; LAYER). Port is **not** complete.

Prior run (2026-09-14T17:40:03Z) STOP'd on `EACCES`. Operator `setfacl` + Architect rematch unblocked this re-dispatch.

## 1. Inventory (verified)

| Prerequisite | Status | Path / evidence |
|--------------|--------|-----------------|
| adevtool device config | OK | `vendor/adevtool/config/device/komodo.yml` → includes `common/gen9pixel.yml` → platform **zumapro**, `build_id: BP4A.260205.002` |
| adevtool device mk | OK | `vendor/adevtool/config/mk/google_devices/device/komodo/{device.mk,BoardConfig-base.mk}` — both include `platform/zumapro/` |
| adevtool vendor skel | OK | `vendor/adevtool/vendor-skels/google_devices/komodo/` (historical snapshot; **not** the live vendor module) |
| vendor spec | OK | `vendor/adevtool/vendor-specs/google_devices/komodo.yml` |
| Kernel tree (partial) | PARTIAL | `device/google/caimito-kernels/6.1/grapheneos/` present. **`trunk-14096387` missing** (see §2, §5) |
| Factory image in `dl/` | **OK (this task)** | `vendor/adevtool/dl/komodo-bp4a.260205.002-factory-aaec4834.zip` (+ backport `komodo-cp1a.260505.005-factory-81a0252b.zip`) |
| Live vendor module | **OK (this task)** | `vendor/google_devices/komodo/` generated by `adevtool generate-all -d komodo` (5733 files; `komodo.mk` 4770 lines) |
| GuardTalk device layer | ABSENT (expected) | No `vendor/guardtalk/device/komodo/` — LAYER task. Do not create here |
| Desktop-flash stamp | ABSENT (expected) | No `releases/desktop-flash/komodo*` |

**Kernel flag (`RELEASE_KERNEL_KOMODO_DIR`)**

| Release config | Value | Tree status |
|----------------|-------|-------------|
| `trunk_staging` (lunch combo below) | `device/google/caimito-kernels/6.1/trunk-14096387` | **MISSING** |
| `cur` | `device/google/caimito-kernels/6.1/grapheneos` | Present |
| `bp4a` | `device/google/caimito-kernels/6.1/25Q4-14305268` | Not used by this lunch target |

Tokay `trunk_staging` points at `…/caimito-kernels/6.1/grapheneos` (present). Komodo lunch on this host fails on the missing trunk dir **after** the live vendor module exists.

**Factory / SPI used**

- Device `build_id` (from `gen9pixel.yml`): `BP4A.260205.002`
- Backport `build_id` (from `pixel.yml`, inherited): `CP1A.260505.005` — `generate-all` fetched this automatically
- Indexed factory (`build-index-main.yml`):

```text
komodo BP4A.260205.002:
  factory: aaec48342537c9d0e8beb8cb9d57a64296ca7f171eb4641e4251312b8c3c469b
           komodo-bp4a.260205.002-factory-aaec4834.zip

komodo CP1A.260505.005:
  factory: 81a0252b6e33bd10da2588f0149e7fff9e11f368df1f7081a9d76b4145843f83
           komodo-cp1a.260505.005-factory-81a0252b.zip
```

- URL pattern: `https://dl.google.com/dl/android/aosp/komodo-bp4a.260205.002-factory-aaec4834.zip`
- Download command (tree root, Node ≥ 24):

```bash
export PATH="/home/oss-c1/.cursor-server/bin/linux-x64/0c32194e3fb5ffaced9fb36430b860ec301e1fc0:$PATH"
# node -v → v24.18.1 (adevtool requires Node 24+)
vendor/adevtool/bin/run download -d komodo -t factory -b BP4A.260205.002
```

adevtool reported SHA-256 check PASS for both zips before unpack.

**Generate note:** First `generate-all` (2026-09-14T17:59:17Z–18:05:25Z) unpacked both factories then **EXIT=1** on:

```text
fatal: detected dubious ownership in repository at
  '.../vendor/adevtool'
git -C vendor/adevtool rev-parse HEAD returned 128
```

That run left a partial tree (no `adevtool-version-check.mk`; `komodo.mk` already `include`s it). **Did not** write `git config`. Retry with session-only env (not a config file change):

```bash
export GIT_CONFIG_COUNT=1
export GIT_CONFIG_KEY_0=safe.directory
export GIT_CONFIG_VALUE_0=/mnt/Big-Storage/GuardTalk/GrapheneOS-worktree/vendor/adevtool
ADEVTOOL_SKIP_DEP_BUILD=1 vendor/adevtool/bin/run generate-all -d komodo
```

Retry (2026-09-14T18:13:06Z–18:13:37Z) **EXIT=0**:

```text
Generated vendor module at vendor/google_devices/komodo
```

Pinned adevtool HEAD in generated `adevtool-version-check.mk`: `5ecaa4df472de43127db378dc803a502271a9ecd`.

`ADEVTOOL_SKIP_DEP_BUILD=1` used on both attempts (akita nsjail pattern). Lunch diagnostic also printed `Build sandboxing disabled due to nsjail error.` — not the lunch FAIL cause.

## 2. Lunch target

| Target | Result | Notes |
|--------|--------|-------|
| Intended lunch | `komodo-trunk_staging-userdebug` | Generated `komodo.mk`: `PRODUCT_NAME := komodo`, `PRODUCT_DEVICE := komodo`, `PRODUCT_MODEL := Pixel 9 Pro XL`; `BUILD_ID` pin `BP4A.260205.002` |
| Product makefile (live) | Present | `vendor/google_devices/komodo/AndroidProducts.mk` |
| `lunch komodo-trunk_staging-userdebug` | **FAIL** | Exact missing path: `device/google/caimito-kernels/6.1/trunk-14096387/vendor_kernel_boot.modules.load` (`RELEASE_KERNEL_KOMODO_DIR` for `trunk_staging`). Make error: `vendor/adevtool/config/mk/google_devices/common/BoardConfig-common-gs201-plus.mk:32: error: vendor_kernel_boot.modules.load not found or empty.` Then `dumpvars failed with: exit status 1`. `TARGET_PRODUCT` remained unset. **Do not treat as PASS.** The lunch bash function still printed `LUNCH_EXIT=0` after the dumpvars panic; product combo was **not** selected. |
| Expected LAYER follow-up | kernel prebuilts | Akita LAYER used a symlink `trunk-14096387` → `grapheneos/`. **Not done in PREFLIGHT.** |

`cmds-for-envsetup.sh`: `export BUILD_ID_komodo="BP4A.260205.002"`.

Lunch on this uid also needs the same `GIT_CONFIG_*` env (or equivalent) so `adevtool-version-check.mk`'s `git -C vendor/adevtool rev-parse HEAD` is not blocked by dubious ownership. Without it, lunch would fail the version-check before the kernel path. This task did **not** add `safe.directory` to git config.

This task did **not** modify `vendor/google_devices/tokay/`, `vendor/google_devices/akita/`, `vendor/guardtalk/device/tokay/`, or `vendor/guardtalk/device/akita/`.

Stamp non-regression (inode/mtime unchanged):

- `releases/desktop-flash/akita-latest` → `akita-20260725-101434` (inode 193110357)
- `releases/desktop-flash/latest` → `tokay-20260725-102506` (inode 193110379)
- There is **no** `tokay-latest` symlink on this tree (only `latest` → tokay stamp). Untouched.

## 3. Zumapro vs tokay — HAL / module / blocklist deltas

Tokay and komodo are the **same SoC family**. Both `includes: [common/gen9pixel.yml]` → `platform: zumapro`, `build_id: BP4A.260205.002`. This is **not** the akita zuma-vs-zumapro split. Do **not** copy akita zuma/Goodix excision.

### 3.1 Platform (adevtool)

| Item | komodo | tokay |
|------|--------|-------|
| `TARGET_BOARD_PLATFORM` | `zumapro` | `zumapro` |
| Init RC package | `init.zumapro.grapheneos.rc` (platform `device.mk`) | same |
| Device mk kernel var | `RELEASE_KERNEL_KOMODO_DIR` | `RELEASE_KERNEL_TOKAY_DIR` |
| BoardConfig-base | `platform/zumapro/BoardConfig-common.mk` | same |
| Perf / SEAPP checks | zumapro `PRODUCT_CHECK_VENDOR_SEAPP_VIOLATIONS` + `PRODUCT_CHECK_DEV_TYPE_VIOLATIONS` | same |
| Display (yml) | 1344 × 2992 | 1080 × 2424 |
| ODM SKUs | GGX8B / GQ57S / GZC4K | G2YBB / G1B60 / GUR25 |

Paths: `vendor/adevtool/config/mk/google_devices/platform/zumapro/`; device fragments under `config/mk/google_devices/device/{komodo,tokay}/`.

### 3.2 Fingerprint HAL (QFP vs Goodix) — from **generated** `komodo.mk`

Evidence is `vendor/google_devices/komodo/komodo.mk` after `generate-all` EXIT=0 (not skel).

| Signal | Generated komodo.mk | Live tokay.mk (comparison only) |
|--------|---------------------|----------------------------------|
| `qfp-daemon` | yes (line 862) | yes |
| `libqfp-service` / `libqfpsuez` | yes (803–804) | yes |
| `adevtool_vintf_fragment_vendor_qfp-daemon.xml` | yes (91) | yes |
| `qfp-daemon.rc` PRODUCT_COPY_FILES | yes (1742) | yes |
| token `goodix` in the `.mk` | **none** | **none** |
| VINTF `qfp-daemon.xml` | live `vintf/vendor/manifest/qfp-daemon.xml` (AIDL `vendor.qti.hardware.fingerprint` / `IQfpExtendedFingerprint`) | tokay live vendor |

`vintf/product/compatibility_matrix.xml` **lists** `vendor.goodix.hardware.biometrics.fingerprint` as a matrix name (lines 236–241). That is **not** a shipped `PRODUCT_PACKAGES` Goodix HAL. Do not treat the matrix entry as Goodix-on-komodo.

**HAL family: QFP (Qualcomm Fingerprint), same as tokay. Not Goodix.** Shared `vendor/guardtalk/feature-excised/fp-excised.mk` already filters **both** QFP and Goodix. Wiring is still LAYER.

### 3.3 Wi‑Fi / BT kernel modules (blocklist)

| Item | komodo (caimito / zumapro) | tokay (zumapro / caimito) |
|------|----------------------------|---------------------------|
| Wi‑Fi driver | `bcmdhd4390` (`init.insmod.komodo.cfg`) | `bcmdhd4390` (`init.insmod.tokay.cfg`) |
| Device insmod re-load | identical module list: `bcmdhd4390`, `snd-soc-cs35l41-i2c`, `syna_touch`, `cs40l26-i2c`, `snd-soc-cs40l26`, `rt6160-regulator` | same |
| Touch | `syna_touch` | `syna_touch` |
| BT `nitrous.ko` | in `vendor_dlkm.modules.load` (caimito grapheneos) | same |
| Upstream blocklist | **same file** `device/google/caimito-kernels/6.1/grapheneos/vendor_dlkm.modules.blocklist` | same |

`init.insmod.komodo.cfg` vs `init.insmod.tokay.cfg`: **comment headers differ** (device name); **module load list is identical**.

GuardTalk shared BT override (tokay-oriented caimito mirror + `blocklist nitrous`) already matches caimito + nitrous. Unlike akita (zuma / `bcmdhd4383` / `goodix_brl_touch`), komodo can use that baseline. LAYER should still copy to `vendor/guardtalk/device/komodo/vendor_dlkm.modules.blocklist` per `REGEN_HOOKS.md`. Do **not** point komodo at an akita/zuma blocklist.

### 3.4 vendor_dlkm blocklist source paths

| Role | Path |
|------|------|
| Komodo / tokay / caiman upstream (grapheneos channel) | `device/google/caimito-kernels/6.1/grapheneos/vendor_dlkm.modules.blocklist` |
| Komodo trunk_staging expected kernel dir | `device/google/caimito-kernels/6.1/trunk-14096387/` (**missing on this host**) |
| GuardTalk shared (tokay-oriented caimito + nitrous) | `vendor/guardtalk/feature-excised/vendor_dlkm.modules.blocklist` |

`REGEN_HOOKS.md` SoC note: **tokay / caiman = zumapro (caimito-kernels 6.1)**. Komodo is the same family. Do not label komodo as zuma or laguna.

### 3.5 Radio / modem

Generated `komodo.mk` ships Shannon / `shared_modem_platform` / `rild_exynos` (same class as tokay). Radio excision remains `vendor/guardtalk/radio-excised/` once LAYER hooks `komodo.mk`. No SoC rename observed in generated package names.

## 4. Residual gaps for `T-PORT-KOMODO-LAYER`

Do **not** start LAYER until this PREFLIGHT is APPROVED.

1. Create `vendor/guardtalk/device/komodo/` + REGEN_HOOKS (zumapro/QFP-correct). Theme via `PRODUCT_DEVICE`. `GUARDTALK_PRODUCT_MODEL` from live `PRODUCT_MODEL` (`Pixel 9 Pro XL`).
2. Per-device `vendor_dlkm.modules.blocklist` = caimito grapheneos baseline + `blocklist nitrous` (shared file is already that baseline; still prefer per-device copy).
3. Kernel prebuilts for `trunk_staging`: missing `device/google/caimito-kernels/6.1/trunk-14096387`. Akita LAYER used a symlink to `grapheneos/`. Confirm before claiming lunch green.
4. Do **not** advertise in `FLASH.md` or the web installer (FLASH card / F-PORT-KOMODO-WEBINSTALL).
5. Optional: tokay `adevtool-version-check.mk` pin drift remains pre-existing; out of this card.
6. Lunch/git on uid `oss-c1`: `vendor/adevtool` dubious ownership. LAYER/FLASH should keep using session `GIT_CONFIG_*` env (do not `git config --global`).

## 5. Verification commands (this run)

Working directory: `/mnt/Big-Storage/GuardTalk/GrapheneOS-worktree`. Node: Cursor `node` v24.18.1.

| Command | EXIT | Notes |
|---------|------|-------|
| `test ! -d vendor/guardtalk/device/komodo` | 0 | layer still ABSENT |
| `ls vendor/adevtool/config/device/komodo.yml` | 0 | |
| `ls vendor/adevtool/config/mk/google_devices/device/komodo/` | 0 | `BoardConfig-base.mk`, `device.mk` |
| `ls -d device/google/caimito-kernels` | 0 | |
| `ls -d device/google/caimito-kernels/6.1/trunk-14096387` | 2 | missing (expected) |
| `ls -d device/google/caimito-kernels/6.1/grapheneos` | 0 | |
| `ls vendor/adevtool/dl/komodo-bp4a.260205.002-factory-*` | 0 | `…-aaec4834.zip` 3.8G |
| `ls vendor/adevtool/dl/komodo-cp1a.260505.005-factory-*` | 0 | `…-81a0252b.zip` 3.8G |
| `vendor/adevtool/bin/run download -d komodo -t factory -b BP4A.260205.002` | 0 | 2026-09-14T17:56:26Z–17:58:51Z |
| `ADEVTOOL_SKIP_DEP_BUILD=1 … generate-all -d komodo` (first) | 1 | git dubious ownership after unpack |
| `ADEVTOOL_SKIP_DEP_BUILD=1 … generate-all -d komodo` (retry + `GIT_CONFIG_*`) | 0 | `Generated vendor module at vendor/google_devices/komodo` |
| `test -d vendor/google_devices/komodo` | 0 | live module present |
| `rg -n "qfp-daemon\|goodix" vendor/google_devices/komodo/komodo.mk` | 0 | qfp-daemon only; no `goodix` token in `.mk` |
| `source build/envsetup.sh && lunch komodo-trunk_staging-userdebug` | dumpvars fail | missing `…/trunk-14096387/vendor_kernel_boot.modules.load`; `TARGET_PRODUCT` unset |

## 6. Explicit non-claims

- Port is **not** complete.
- GuardTalk komodo device layer **not** applied (`vendor/guardtalk/device/komodo/` still ABSENT).
- Lunch is **not** green. Missing kernel dir is documented, not invented PASS.
- Full `m` / desktop-flash bundle **not** in scope.
- `FLASH.md` and `vendor/guardtalk/web-installer/` were **not** edited and still do not name komodo.
- caiman / rango were **not** touched.
- No USB flash.
- No git commit / push.
- No `git config` writes.
